defaults:
  - _self_
  - optimizer/adamw.yaml

dataset: cifar100
conv_layers: 3
lin_layers: 2
epochs: 50
loss_fn:
  _target_: torch.nn.CrossEntropyLoss

hydra:
  run:
    dir: .
  # mode: MULTIRUN
  # sweeper:
  #   params:
  #     optimizer: optimizer/adamw-lo, optimizers/adamw, optimizers/adamw-hi
  #     conv_layers: 2, 3, 5, 8, 13
  #     lin_layers: 2, 3, 4
